<h3>Comboios</h3>

{{#each comboios}}
<div class="card">
    <div class="card-body">
        <label>Nome do Comboio: {{comboioNome}}</label>
        <a class="btn btn-primary float-end" href="/formulario/comboiosVizu/{{_id}}">Visualizar</a>
    </div>
</div>
{{else}}
<h5>Nenhum comboio cadastrado</h5>
{{/each}}
<br>
<a href="/comboios/{{previousPage}}" class="btn btn-primary">Anterior</a>
<a href="/comboios/{{nextPage}}" class="btn btn-primary" style="float: right" {{hidden}}>Próximo</a>